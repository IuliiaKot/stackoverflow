<header class='header-count-answer'>
  <%= question.answers.count%> Answers
</header>

  <article class="collection-of-answers">
    <% question.answers.each do |answer|%>
      <%= erb :'answers/_answer', layout: false, locals: {answer: answer}%>
    <% end %>
  </article>
